# Working on Recall

The instructions live in `CLAUDE.md`. Read that file.

This one is a pointer so that tools looking for `AGENTS.md` find something correct instead
of writing their own copy. Do not paste the contents of `CLAUDE.md` in here. Two
instruction files drift apart, and a stale one is worse than none, for the same reason the
extraction prompt is never forked per platform.
